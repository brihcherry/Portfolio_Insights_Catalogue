// Route path constants. Keep these in sync with the routes in pages/Router.tsx.
//
// Convention: store bare path segments with no leading slash (e.g. "login", not "/login").
// These are nested child routes, so a leading slash isn't needed — keep them all relative
// for consistency.

export const ROUTE_PATH_LOGIN_PAGE = "login";
